meta {
  name: 1 - Login (Patient)
  type: http
  seq: 1
}

post {
  url: {{node_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "patient@example.com"
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.access_token);
}

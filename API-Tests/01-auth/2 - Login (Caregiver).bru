meta {
  name: 2 - Login (Caregiver)
  type: http
  seq: 2
}

post {
  url: {{node_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "caregiver@example.com"
  }
}

script:post-response {
  bru.setEnvVar("caregiver_token", res.body.access_token);
}
